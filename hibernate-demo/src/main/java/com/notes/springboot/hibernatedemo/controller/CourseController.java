package com.notes.springboot.hibernatedemo.controller;

import com.notes.springboot.hibernatedemo.Entity.Course;
import java.util.*;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class CourseController {

/*    @RequestMapping("/courses")
    public List<Course> retrieveAllCources(){
        return Arrays.asList(new Course(1,"Learn Aws","In28minutes"),
         new Course(2,"Learn Physics","Belal"));*/
    //}
}
